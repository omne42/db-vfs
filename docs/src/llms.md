# `llms.txt`

Inspired by the AI SDK docs’ `llms.txt`, this repo includes a single-file documentation bundle for
LLM/RAG ingestion: `llms.txt` (Markdown in a `.txt` file).

Regenerate it after documentation changes:

```bash
./scripts/llms.sh
```
